# Copilot Instructions

## Project Guidelines
- User prefers not to rely on separate runtime installations on host machines and wants self-contained binaries (avoid external CRT redistributable dependency).